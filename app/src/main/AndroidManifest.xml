<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        AndroidManifest.xml — The App's "ID Card"

        Play Store reads this file to understand your app.
        Key things here:
          • android:label      — App name shown on the device
          • android:icon       — Launcher icon (add via Android Studio → Image Asset)
          • android:theme      — Must be NoActionBar so Compose controls the full screen
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EMICalculator">

        <!--
            The activity uses the Splash theme so the OS draws the splash window
            BEFORE our Compose content inflates. MainActivity calls installSplashScreen()
            which then hands off to the postSplashScreenTheme (Theme.EMICalculator).
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EMICalculator.Splash"
            android:windowSoftInputMode="adjustResize">

            <!-- This intent-filter marks MainActivity as the launcher screen -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FileProvider — lets us share the generated PDF with other apps.

            android:authorities must match the string used in PdfGenerator.kt:
              "${context.packageName}.provider"

            android:exported="false"  → other apps cannot browse our files directly,
                                        they can only open a specific Uri we hand them.
            android:grantUriPermissions="true" → we grant temporary read access via the Intent.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
